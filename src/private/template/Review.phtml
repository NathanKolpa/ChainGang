<?php
$votedUp = false;
$votedDown = false;

if(isset($data["user"]))
{
    $isUP;
    if($review->hasUserVoted($data["user"], $isUP))
    {
        if($isUP)
        {
            $votedUp = true;
        }
        else
        {
            $votedDown = true;
        }
    }
}

?>

<div class="mx-auto col-sm-9">
    <p class="h3"><?=$review->getTitle()?></p>
    <p><?=$review->getText()?></p>
    <ul class="list-inline">

        <li class="list-inline-item <?=$votedUp? "VoteSelected" : "" ?>" onclick="voteButton(this)" id="up_<?=$review->getId()?>">
            <i class="fa fa-arrow-up"></i>
        </li>

        <li class="list-inline-item" id="count_<?=$review->getId()?>">
            <p><?=$review->getKarma()?></p>
        </li>

        <li class="list-inline-item <?=$votedDown? "VoteSelected" : "" ?>" onclick="voteButton(this)" id="down_<?=$review->getId()?>">
            <i class="fa fa-arrow-down"></i>
        </li>
    </ul>
</div>